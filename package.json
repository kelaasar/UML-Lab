{
  "scripts": {
    "test": "cd client && npm run test && cd ../server && npm run test",
    "doc": "jsdoc -r -c jsdoc.json && echo .name code { color: #ffffff; } >> docs\\styles\\jsdoc-default.css",
    "install-dependencies": "npm install && cd client && npm install && cd ../server && npm install && cd .."
  },
  "dependencies": {
    "axios": "^1.6.7",
    "dotenv": "^16.4.1",
    "firebase": "^10.7.2",
    "jest": "^29.7.0",
    "openai": "^4.26.0",
    "sinon": "^17.0.1",
    "supertest": "^6.3.4"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "jsdoc": "^4.0.2"
  }
}
